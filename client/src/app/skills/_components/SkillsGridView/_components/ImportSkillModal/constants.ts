/** Constants for ImportSkillModal. */

/** The only two formats the import route accepts (.tar/.rar/.7z are out of scope). */
export const ACCEPTED_EXTENSIONS = ".md,.zip";

/** Modal width (px) — wide enough for the rendered body preview. */
export const MODAL_WIDTH = 680;
