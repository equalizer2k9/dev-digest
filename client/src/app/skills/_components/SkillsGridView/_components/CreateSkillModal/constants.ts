import type { SkillType } from "@devdigest/shared";

/** Constants for CreateSkillModal. */

/** Type a from-scratch skill starts as. */
export const DEFAULT_TYPE: SkillType = "custom";

/** Modal width (px). */
export const MODAL_WIDTH = 620;
