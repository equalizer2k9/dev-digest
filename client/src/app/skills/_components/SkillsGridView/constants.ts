/** Constants for SkillsGridView. */

/** Responsive card grid template — same auto-fill rhythm as /agents. */
export const CARD_GRID_COLS = "repeat(auto-fill, minmax(280px, 1fr))";

/** Placeholder cards rendered while GET /skills is in flight. */
export const SKELETON_CARDS = 3;
